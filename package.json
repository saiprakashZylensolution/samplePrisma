{
  "name": "sample_prisma",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "dev": "npm run generate && npm run migrate",
    "generate": "npx prisma generate",
    "migrate": "npx prisma migrate dev --name init"
  },
  "dependencies": {
    "@prisma/client": "^5.11.0",
    "express": "^4.18.3",
    "nodemon": "^3.1.0"
  }
}
